import { startServiceDueChecker } from '../../lib/serviceDueChecker';

// Start the service due checker when the application starts
startServiceDueChecker();

export default function handler() {
  // This is just a placeholder function
  // The actual work is done when the file is imported
} 